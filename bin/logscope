#!/usr/bin/env python3
import pathlib
import sys

PROJECT_ROOT = pathlib.Path(__file__).resolve().parents[1]
if str(PROJECT_ROOT) not in sys.path:
    sys.path.insert(0, str(PROJECT_ROOT))

from logscope.app.cli import main


if __name__ == "__main__":
    raise SystemExit(main())
